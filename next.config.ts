import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Markdown source documents live outside the app tree (see src/lib/documents.ts).
  // Nothing special needed here yet; kept explicit for future config.
};

export default nextConfig;
